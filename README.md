# micrograd

A copy of https://github.com/karpathy/micrograd for learning purposes.
Ported to clojure to make it more interesting.

## Usage

For now this simply works in the repl.

Uses the same interface as the micrograd this is copying, which itself is based on PyTorch.

